import styled from "styled-components";

export const OurStoryCardText = styled.div`
    margin: 0 auto;
    max-width: 950px;
    padding: 75px 0;
    position: relative;
    text-align: center;
    width: 100%;
    z-index: 1;
    ${({ restStyle }) => restStyle}
`;
